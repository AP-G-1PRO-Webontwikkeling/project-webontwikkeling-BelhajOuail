<!DOCTYPE html>
<html lang="nl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" href="data:image/x-icon;,">
    <style>
        body {
            background-color: #f0f0f0;
        }

        .card {
            padding: 1rem;
        }

        .card-title {
            padding: 20px;
            margin: 0;
        }

        .btn {
            margin-top: 2rem;
            padding: 0.5rem;
        }
        .error-message{
            font-weight: bolder;
            color: red;
        }
    </style>
</head>

<body class="text-center">

    <div class="container d-flex align-items-center justify-content-center" style="height: 100vh;">
        <div class="card shadow-sm w-100" style="max-width: 450px;">
            <div class="card-header bg-primary text-white">
                <h3 class="card-title">Welkom bij de Terminal App!</h3>
            </div>
            <div class="card-body">
                <h5 class="card-title mb-4">Log in met uw gebruikersnaam en wachtwoord</h5>

                <form method="POST" action="/login">
                    <div class="form-group">
                        <label for="username">Gebruikersnaam</label>
                        <input type="text" class="form-control" id="username" name="username" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Wachtwoord</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>

                    <% if (typeof message !== 'undefined') { %>
                        <p class="error-message"><%= message %></p>
                    <% } %>
                    
                    <button type="submit" class="btn btn-primary w-50 mx-auto d-block">Inloggen</button>

                    <a href="/register" class="btn btn-secondary w-50 mx-auto d-block mt-4">Registreren</a>
                </form>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
