<!DOCTYPE html>
<html lang="nl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Finance Tracker</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" href="data:image/x-icon;,">
    <style>
        body {
            background-color: #f0f0f0;
        }

        .card {
            padding: 1rem;
        }

        .card-title {
            padding: 20px;
            margin: 0;
        }


        .btn {
            margin-top: 3rem;
            padding: 0.5rem;
        }
    </style>
</head>

<body class="text-center">

    <div class="container d-flex align-items-center justify-content-center" style="height: 100vh;">
        <div class="card shadow-sm w-100" style="max-width: 550px;">
            <div class="card-header bg-primary text-white">
                <h3 class="card-title">Welkom, <%= user.name %>
                </h3>
            </div>
            <div class="card-body">
                <h5 class="card-title mb-4">Voeg een nieuwe transactie toe of bekijk ze.</h5>

                <form method="POST" action="/transactions/<%= user.id %>/add">
                    <div class="form-group">
                        <label for="description">Omschrijving</label>
                        <input type="text" class="form-control" id="description" name="description" required>
                    </div>
                    <div class="form-group">
                        <label for="amount">Bedrag (€)</label>
                        <input type="number" class="form-control" id="amount" name="amount" required>
                    </div>
                    <div class="form-group">
                        <label for="category">Categorie</label>
                        <input type="text" class="form-control" id="category" name="category" required>
                    </div>
                    <div class="form-group">
                        <label for="isIncoming">Is dit een inkomst?</label>
                        <select class="form-control" id="isIncoming" name="isIncoming">
                            <option value="true">Ja</option>
                            <option value="false">Nee, het is een uitgave</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary w-50 mx-auto d-block">Voeg Transactie Toe</button>

                    <a href="/transactions/<%= user.id %>" class="btn btn-secondary w-50 mx-auto d-block mt-4">Bekijk
                        Transacties</a>
                </form>
            </div>
            <form action="/logout" method="get">
                <button type="submit">Log uit</button>
            </form>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>